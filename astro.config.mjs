import { defineConfig } from 'astro/config';

export default defineConfig({
  // Set this to the live domain before deploying (used for canonical URLs / OG tags).
  site: 'https://rufarchitecture.com',
});
